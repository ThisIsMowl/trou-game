const defaultState = {
  counterPosition: 0,
}

const gameplayReducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default gameplayReducer
